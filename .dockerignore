# Git
.git
.gitignore
.gitattributes

# Python
__pycache__
*.py[cod]
*$py.class
*.so
.Python
*.egg-info
dist
build
.eggs
.pytest_cache
.venv
venv
env

# IDE
.vscode
.idea
*.swp
*.swo
*~

# Data (will be mounted as volume)
# Note: src/data is NOT excluded as it's needed during build for dataset download
data
data/
data/**
!data/.gitkeep

# Models (will be mounted as volume)
models/checkpoints
models/checkpoints/
models/checkpoints/*
!models/checkpoints/.gitkeep

# Logs (will be mounted as volume)
logs
logs/
logs/**
*.log

# Runtime files
*.pid
streamlit.pid

# Documentation
docs/
# README.md is needed for package build
# LICENSE
TODO.md

# Notebooks (exclude only notebook files, not the directory structure)
*.ipynb
.ipynb_checkpoints
# Keep src/notebooks/ structure but exclude notebook files via *.ipynb

# Scripts (not needed in container, except load_dataset.sh)
scripts/*
!scripts/load_dataset.sh
start.sh
stop.sh

# Docker
Dockerfile
docker-compose.yml
.dockerignore

# Tests
tests/

# Large application data
app_display_data/

# UV lock file (will be regenerated)
# Keep pyproject.toml but exclude lock for faster builds
# uv.lock
